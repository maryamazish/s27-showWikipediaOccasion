import { Link } from "react-router-dom";
import Title from "./../general/title";

const Header = () => {
  return (
    <div className="center">
      <Title text="Show Events"></Title>
    </div>
  );
};

export default Header;
